<%- include('partials/header') %>
    <main>
        <%= title %>
        <ul>
            <% categories.forEach(category=> { %>
                <li>
                    <a href=""><%= category.name %></a>
                </li>
                <% }); %>
        </ul>
    </main>
    <%- include('partials/footer') %>